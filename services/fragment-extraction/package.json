{
  "name": "@cronus/fragment-extraction",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "fluent-ffmpeg": "*",
    "sharp": "*",
    "@cronus/domain": "workspace:*",
    "@cronus/db": "workspace:*",
    "@cronus/storage": "workspace:*",
    "@cronus/logger": "workspace:*"
  }
}

{
  "name": "@cronus/natural-center",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "*",
    "@cronus/domain": "workspace:*",
    "@cronus/db": "workspace:*",
    "@cronus/logger": "workspace:*"
  }
}
